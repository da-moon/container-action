name: "container action"
description: "some action"
author: "da-moon"


inputs:
  myInput:
    description: "sominput"
    required: true
    default: "default input"
 git_repository_owner:
    description: "repository owner"
    required: true
    default: "da-moon"
outputs:
  myOutput:
    description: "some output"
runs:
  using: "docker"
  image: "Dockerfile"
